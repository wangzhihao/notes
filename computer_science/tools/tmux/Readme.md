Sometimes you need to update the export environment variables but tmux tends to cache them. A violent but effective way
is to call `tmux kill-server` then the new updates will be took into consideration.
